name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    uses: danielriddell21/github-actions/.github/workflows/release.yaml@v2
    permissions:
      contents: write
    with:
      # Answers the archive-layout hook; letsgo.mod pins it by digest.
      plugins: letsgo-multi
    secrets:
      tap-app-private-key: ${{ secrets.TAP_APP_PRIVATE_KEY }}
